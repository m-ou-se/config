[diff]
	tool = vimdiff

[color]
	ui = true

[alias]
	l     = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative --date-order
	lg    = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative --date-order --all
	stat  = status
	st    = status
	ff    = merge --ff-only
	index = diff --cached

[core]
	excludesfile = ~/.gitignore
	whitespace = space-before-tab

[push]
	default = simple

[include]
	path = ~/.gitconfig.local

[push]
	default = current

[merge]
	conflictstyle = diff3
